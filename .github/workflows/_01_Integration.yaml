name: 01 - Integration

on:
    pull_request:
        branches:
            - develop

jobs:
    Test_Back_Front:
        uses: ./.github/workflows/01-1_TestBackFront.yaml

    Quality_Sonar_Qube:
        needs: [Test_Back_Front]
        uses: ./.github/workflows/01-2_SonarQube.yaml
        secrets: inherit

    Security_Snyk:
        needs: [Quality_Sonar_Qube]
        uses: ./.github/workflows/01-3_Snyk.yaml
        secrets: inherit

